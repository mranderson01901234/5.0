# @llm-gateway/shared

Shared types, Zod schemas, and OpenAPI generation for the LLM Gateway.

## Commands

- `pnpm build` - Build TypeScript and generate OpenAPI JSON
- `pnpm lint` - Run ESLint
- `pnpm typecheck` - Type check without emitting

